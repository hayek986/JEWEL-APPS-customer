workflows:
  simulator-build:
    name: Simulator Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - flutter pub get
      - flutter build ios --simulator --no-codesign
    artifacts:
      - build/ios/**/*.app  # <-- هذا هو التعديل الرئيسي
    publishing:
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: false
