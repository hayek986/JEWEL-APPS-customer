name: hayekjewel
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Firebase (أحدث إصدارات متوافقة مع Flutter 3.x)
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  cloud_firestore: ^6.0.0
  cloud_functions: ^6.0.0
  firebase_storage: ^13.0.0
  firebase_messaging: ^16.0.0
  
  # باقي المكتبات
  provider: ^6.1.2
  http: ^1.2.2
  html: ^0.15.4
  image_picker: ^1.1.2
  marquee: ^2.2.3
  photo_view: ^0.15.0
  cached_network_image: ^3.4.0
  shared_preferences: ^2.3.2
  flutter_dotenv: ^5.1.0
  carousel_slider: ^5.0.0
  flutter_local_notifications: ^19.4.0
  uuid: ^4.5.1
  geolocator: ^12.0.0
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/bk.png
